import { Paper } from "@mui/material";

const DocSelves = () => {
  return (
    <Paper
      sx={{
        flex: 1.5,
        height: "calc(100vh - 50px)",
      }}
    />
  );
};

export default DocSelves;
